spring:
  application:
    name: config-client
  cloud:
    config:
      uri:  http://localhost:8001
      label: master
      profile: dev

management:
  security:
      enabled: false

server:
  port: 8002